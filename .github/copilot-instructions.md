# 全局规则

- **严格遵循 system_arch.md 中的 outline**，不要修改架构、目录结构或模块边界。
- 程序运行环境都在 `conda activate HR` 下。
- 如果需求或上下文不清楚，不允许默认假设，务必询问以获得澄清。
- 不要忽略错误、异常、缺失依赖或需求矛盾。

# 任务拆分与执行方式

- 在更改代码前必须先有完整的 todo_list 计划，并按 todo 推进。
- 每次专注于一个模块或问题，每个模块必须经过测试。
- 确保关键模块的输出和决策点以清晰、结构化的消息展示到终端，以便于调试和执行跟踪。

